import styled from 'styled-components';
import tw from 'twin.macro';

export const Container = styled.div`${tw`
flex justify-between gap-[1px]
p-2

`}`
export const Heading = styled.div`${tw`
text-[1rem] font-semibold

`}`