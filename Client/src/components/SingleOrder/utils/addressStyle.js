import styled from 'styled-components';
import tw from 'twin.macro';

export const Container = styled.div`${tw`
flex-1
flex flex-col gap-2
bg-white
py-4 p-2
`}`
export const Name = styled.div`${tw`

`}`
export const AddressInfo = styled.div`${tw`
text-gray-500
`}`
export const Contact = styled.div`${tw`
text-gray-500
`}`