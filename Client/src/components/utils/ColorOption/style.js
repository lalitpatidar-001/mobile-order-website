import styled from 'styled-components';
import tw from 'twin.macro';

export const Container = styled.div`${tw`
bg-[#dddddd]
flex 
w-fit
bg-white
-mt-4
`}`;
export const ColorBox = styled.div`${tw`
h-[70px]
p-[2px]
rounded
cursor-pointer
overflow-hidden
`}
border: 1.5px solid lightgray
`;
export const ColorImg = styled.img`${tw`
w-[70px]
h-full

`}

`;